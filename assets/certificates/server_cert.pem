# Placeholder for server certificate
# To implement certificate pinning:
# 1. Obtain the server certificate using:
#    openssl s_client -connect admin.ota.gov.et:443 -servername admin.ota.gov.et < /dev/null | openssl x509 -outform PEM > server_cert.pem
# 2. Replace this file with the actual certificate
# 3. Update SecurityUtils._loadPinnedCertificate() to properly parse the certificate